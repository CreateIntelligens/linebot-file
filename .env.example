# Runtime config for local/dev or VM docker run
PORT=5000

# LINE bot credentials
ChannelSecret=
ChannelAccessToken=

# Google OAuth (user consent)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Must match OAuth client settings and your reverse proxy TLS domain
GOOGLE_REDIRECT_URL=https://ptt.line.5gao.ai:5000/oauth/callback
# MongoDB (compose: internal mongo service)
MONGO_INITDB_ROOT_USERNAME=appuser
MONGO_INITDB_ROOT_PASSWORD=changeme-please
MONGODB_URI=mongodb://appuser:changeme-please@mongo:27017/?authSource=admin
MONGODB_DB=linebot_file